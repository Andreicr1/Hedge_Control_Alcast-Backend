# Example only. Do NOT commit real secrets.
# Copy to backend/.env (which is gitignored) and fill values.

# Render should set these as environment variables instead of using a file.
ENVIRONMENT=development

# Apply Alembic migrations automatically on app startup.
# Recommended true on Render to keep schema up to date.
RUN_MIGRATIONS_ON_START=true

# Supabase connection string example:
# postgresql://postgres:<PASSWORD>@db.<PROJECT_REF>.supabase.co:5432/postgres
DATABASE_URL=postgresql://postgres.msegkrybfzlegbbwqjxu:[YOUR-PASSWORD]@aws-1-sa-east-1.pooler.supabase.com:5432/postgres

# Must be set to a strong value.
SECRET_KEY=dev-local-only__replace-if-shared__please-change

# In production you must explicitly set allowed origins.
# Example JSON array:
# CORS_ORIGINS=["https://your-vercel-app.vercel.app","http://localhost:5173"]
