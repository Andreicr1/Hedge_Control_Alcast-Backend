# DEPRECATED
#
# Render reads blueprint files from the repository root ("render.yaml").
# This file exists only to avoid confusion for contributors who may look under
# "backend/" first.
#
# Use the root file instead:
# - ./render.yaml

# (intentionally empty)
